#install.packages(param)
#instala paquetes, dependiendo el nombre que reciba
#como parametro es el paquete que te instalara
install.packages("ggplot2")

#library(ggplot2)
#carga el paquete de la libreria
#que recibe como parametro
library(ggplot2)

#cuando te salga un mensaje dale a NO >:C
install.packages("dplyr")
library(dplyr)

#version 
#retorna la version que es R asi como otros tipos
#de datos
version
